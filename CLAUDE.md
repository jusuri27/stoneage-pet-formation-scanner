# 프로젝트: LDPlayer 게임 랭킹 자동 화면 캡처

## 주석 작성 규칙
- 함수마다 목적을 설명하는 주석을 작성한다.
- 복잡한 로직은 "왜 이렇게 구현했는지"를 설명한다.
- 코드만 봐도 알 수 있는 내용은 주석으로 작성하지 않는다.
- 변수의 역할이 명확하지 않으면 주석을 추가한다.
- 한 줄 주석은 '#'을 사용한다.

## 코딩 규칙
- 예외 처리 필수 (이미지 매칭 실패, 화면 캡처 실패, 클릭 좌표 범위 오류 등)

## 작업 방식
- 새 기능을 추가하기 전에 먼저 계획을 설명하고 확인받은 후 코드를 작성한다
- 파일 하나씩 순차적으로 작업한다 (여러 파일 동시 대량 수정 금지)
- 테스트 가능한 함수 단위로 작게 나눠서 작성한다

## 작업 완료 후 커밋 규칙
- _bak 파일은 커밋하지 않는다
1. 요청받은 코드 수정을 완료하면 결과만 보고하고 멈춘다. 커밋을 먼저 제안하지 않는다.
2. 사용자가 동작을 확인한 후 "커밋해줘" 등으로 명시적으로 요청하면 그때 커밋 절차를 시작한다.
3. 커밋 절차: `git diff`로 변경 내용을 확인 → 내용에 맞는 커밋 메시지 초안 작성 → 사용자에게 보여주고 승인받은 뒤 커밋한다.
4. 커밋 메시지 형식은 Conventional Commits를 따른다.
   - feat: 새 기능 추가
   - fix: 버그 수정
   - refactor: 동작 변화 없는 코드 개선
   - docs: 문서 수정
   - chore: 빌드/설정 등 잡무
   - test: 테스트 추가/수정
   - 예: `fix: 로그인 시 토큰 만료 체크 로직 오류 수정`
5. 커밋 메시지는 한글로 작성하고, 제목은 50자 이내, 필요시 본문에 변경 이유를 2~3줄로 추가한다.
6. 여러 개의 논리적으로 다른 변경사항이 섞여 있으면 하나로 뭉치지 말고 파일/기능 단위로 나눠서 커밋한다.
7. 커밋 전에는 반드시 `git status`로 의도치 않은 파일(빌드 산출물, .env 등)이 포함되지 않았는지 확인한다.
8. push는 별도 지시가 있을 때만 한다.


